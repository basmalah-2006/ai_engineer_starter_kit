# 🤖 Smart Study Buddy — Conversational AI Agent with Tool Calling, Memory & RAG

A production-grade conversational assistant built as **Mini Project 7** for the Helwan Career Center 12-Week Industry Roadmap (Week 7: Tool Calling, Memory & Conversational Systems).
The agent combines **Gemini function calling**, a **dual-layer memory system**, and **RAG over 706 pages of AI lecture notes** into one coherent assistant that doesn't just talk — it *does things*.

The project ships with two interfaces: a **CLI agent** (`main.py`) and a polished **Streamlit web app** (`app.py`) ready for free cloud deployment.

> **🌐 Live Demo:** [https://YOUR-APP-NAME.streamlit.app](https://share.streamlit.io/) ← *(replace with your deployed URL)*

---

## ✨ Features

- **🔧 Autonomous Tool Calling:** The agent decides *by itself* when to search the course materials or compute grades — no hardcoded routing.
- **🔁 Self-Retrying Retrieval:** In the demo below, the agent called the search tool **twice** because it judged the first retrieval insufficient — emergent agentic behavior.
- **🧠 Dual-Layer Memory:** Rolling short-term window (last 10 messages) + persistent long-term facts (`user_memory.json`) that survive restarts.
- **📚 Grounded RAG Answers:** Every course-related answer is retrieved from a 706-page lecture PDF and cited with exact page numbers.
- **🛡️ Production-Grade Safety:** Tool allow-list, infinite-loop guard, retry-with-backoff on rate limits, and protobuf-safe input validation.
- **🌍 Bilingual:** Understands and replies in Arabic or English.
- **💸 Zero-Cost Embeddings:** RAG runs on a local HuggingFace model — no paid embedding API, no quota surprises.

---

## 🎬 Demo Conversation Transcript

*(Required deliverable — real, unedited run)*

```text
👤 You: Hello, my name is Basmalah and I study Computer Engineering.
💾 [Saved to long-term memory]
🤖 Assistant: Hello Basmalah! It's great to meet you. As a Computer Engineering
   student, how can I help you with your studies today?

👤 You: Briefly explain the A* Search algorithm.
⚙️  [Agent calling tool: 'search_course_materials']
🔧 [Tool Result]: --- [Source 1: Page 174] --- Search: Basic idea ...
⚙️  [Agent calling tool: 'search_course_materials']   ← agent judged retrieval
                                                        insufficient and re-searched
🔧 [Tool Result]: --- [Source 1: Page 270] --- Informed Search [Heuristic Search] ...
🤖 Assistant: The **A* Search algorithm** is a widely used pathfinding and graph
   traversal algorithm in artificial intelligence.
   * Evaluation Function: f(n) = g(n) + h(n), where g(n) is the exact cost to
     reach the node and h(n) is the heuristic estimated cost to the goal.
   * Optimality & Completeness: guaranteed to find the shortest path if the
     heuristic is admissible (never overestimates).
   *(Source: Pages 174–176, 270)*

👤 You: My quiz grades were 85, 90, and 95. What is my average?
⚙️  [Agent calling tool: 'calculate_average']
🔧 [Tool Result]: {"average": 90.0, "count": 3, "grades": [85.0, 90.0, 95.0]}
🤖 Assistant: Your average quiz grade is **90.0**! Great job, Basmalah!

👤 You: Do you remember my name and what I study?
🤖 Assistant: Yes, of course, Basmalah! You told me that your name is Basmalah
   and that you study Computer Engineering.
```

---

## 🧰 Tool Definitions (LO 7.1, 7.2)

| Tool | Purpose | Parameters | Validation & Failure Mode |
|---|---|---|---|
| `search_course_materials` | Semantic search over 706 pages of CS212 AI lecture notes | `query: string` | Coerced with `str()`; returns explicit "not found" message on empty retrieval |
| `calculate_average` | Arithmetic mean of numerical grades | `grades: array<number>` | Coerced with `float()` (protobuf-safe); rejects empty lists; errors returned as JSON |

Schemas are **auto-generated by Gemini from Python docstrings + type hints**, keeping definitions and implementations in a single source of truth. A portable OpenAI-style schema is also provided in `tools.py` with an allow-list dispatcher:

```python
AVAILABLE_FUNCTIONS = {
    "search_course_materials": search_course_materials,
    "calculate_average": calculate_average,
}
# Any tool name outside this allow-list is rejected BEFORE execution (LO 7.5)
```

---

## 🧠 Memory Architecture (LO 7.3)

| Layer | Storage | Scope | Injection Point |
|---|---|---|---|
| **Short-term** | In-memory rolling window (last 10 messages) | Current session | Passed as Gemini `chat history` |
| **Long-term** | `user_memory.json` (persistent fact list) | Across sessions & restarts | Injected into the `system_instruction` on **every turn** |

Facts are captured with a lightweight keyword heuristic (`my name is`, `i study`, `اسمي`, `أدرس`…), deduplicated, and persisted immediately:

```python
Known user facts: Hello, my name is Basmalah and I study Computer Engineering.
```

The 10-message cap on short-term memory prevents context overflow and keeps token costs bounded.

---

## 📚 RAG Pipeline (LO 7.4)

```
CS212 Lecture PDF (706 pages)
        │
        ▼
pypdf extraction (+ page metadata)      → 705 text pages
        │
        ▼
RecursiveCharacterTextSplitter           → 738 chunks
        │
        ▼
sentence-transformers/all-MiniLM-L6-v2   → free, local, CPU-friendly
        │
        ▼
FAISS index (persisted: vector_store/)
        │
        ▼
top-3 similarity search → context with page citations → Gemini
```

| Parameter | Value | Rationale |
|---|---|---|
| Chunk size | 1000 chars | Lecture slides are dense bullet lists |
| Chunk overlap | 200 chars | Concepts span slide boundaries |
| Metadata | `page`, `chunk_index` | Enables page-level citations |
| Embedding model | `all-MiniLM-L6-v2` | Zero API cost; avoids quota exhaustion on large corpora |
| top-k | 3 | Balances context richness vs. noise |

---

## 🤖 The Agent Loop — call → execute → respond (LO 7.1, 7.5)

```
User message
     ▼
Gemini Flash (tools bound)
     ▼
function_call? ──No──▶ final text ▶ memory update ▶ UI
     │ Yes
     ▼
Allow-list check ▶ execute tool inside try/except
     ▼
FunctionResponse (role="USER") ▶ back to model
     ▼
(loop, guarded by MAX_TOOL_ITERATIONS = 4)
```

---

## 🛡️ Safety, Reliability & Error Handling (LO 7.5)

*Portfolio emphasis: tool calling + memory is the bridge to full agents — reliability first.*

- **Allow-list authorization:** only explicitly registered tools can ever execute.
- **Infinite-loop guard:** tool chains terminate after 4 iterations with a graceful user-facing message.
- **Retry-with-backoff:** 429 quota payloads are parsed for their `retry_delay` seconds; the agent sleeps and retries (max 3) instead of crashing.
- **Protobuf-safe coercion:** Gemini's `RepeatedComposite` grade lists are converted to plain floats before JSON serialization.
- **Safe response parsing:** part-level scanning avoids the `Could not convert part.function_call to text` crash on tool-only responses.
- **Graceful tool failures:** tools return structured `{"error": ...}` JSON so the model can recover conversationally.
- **Secret hygiene:** `.env` is gitignored; cloud deployments read the key from Streamlit Secrets.

---

## 🧪 Engineering Challenges & Solutions (the real debugging journey)

| Challenge | Root Cause | Fix |
|---|---|---|
| `404 model not found` | Gemini 1.5/2.5 retired for new API keys | Wrote `check_models.py` to discover live models programmatically → switched to Flash-lite |
| `400 Role 'function' not supported` | Gemini 3.x API role change | Send `FunctionResponse` with `role="USER"` |
| `Could not convert part.function_call to text` | `.text` accessed on tool-only responses | Part-level `function_call` scanning helpers |
| `429 quota exceeded (5 req/min)` | Free-tier rate limit | Retry-with-backoff parsing the server-suggested delay |
| OpenAI credits exhausted mid-build | Paid embedding API | Migrated embeddings to local HuggingFace (zero cost) |
| `RepeatedComposite is not JSON serializable` | Protobuf list types | `[float(g) for g in grades]` coercion |
| Windows paths break on Linux cloud | `r"week7\data\..."` backslashes | POSIX forward slashes for cross-platform deployment |

**Key lesson:** added complexity (retries, guards, coercion) is exactly what separates a *demo* from a *production system*.

---

## 📁 Project Structure

```
ai_engineer_starter_kit/
├── .env                          ← GOOGLE_API_KEY (gitignored)
├── requirements.txt
├── README.md
└── week7/
    ├── app.py                    ← Streamlit web UI (deployable)
    ├── main.py                   ← CLI agent loop
    ├── memory.py                 ← Short/long-term memory manager
    ├── rag.py                    ← Ingestion + FAISS retrieval
    ├── tools.py                  ← Portable schemas + allow-list dispatcher
    ├── test_tools.py             ← Independent tool unit tests
    ├── check_models.py           ← Live model discovery utility
    ├── user_memory.json          ← Persistent long-term facts
    ├── vector_store/             ← Persisted FAISS index
    └── data/
        └── CS212-...-Spring-2025.pdf   ← 706-page source corpus
```

---

## 🚀 Installation & Running

### 1. Clone & set up
```bash
git clone https://github.com/basmalah-2006/ai_engineer_starter_kit.git
cd ai_engineer_starter_kit
python -m venv venv
venv\Scripts\activate        # Windows
pip install -r requirements.txt
```

### 2. Add your API key
Create `.env` in the repository root:
```env
GOOGLE_API_KEY=your_key_here
```
*(Free key from [Google AI Studio](https://aistudio.google.com/app/apikey))*

### 3. Build the knowledge base (one-time, ~2 min)
```bash
python week7/rag.py
```

### 4. Run the CLI agent
```bash
python week7/main.py
```

### 5. Or launch the web UI
```bash
streamlit run week7/app.py
```

---

## ☁️ Deployment (Streamlit Community Cloud — free)

1. Push the repo (including `vector_store/` and the PDF).
2. On [share.streamlit.io](https://share.streamlit.io/): **New app** → repository → branch → main file path `week7/app.py`.
3. Add `GOOGLE_API_KEY` under **Advanced settings → Secrets** (never in code).
4. Deploy — the app loads the FAISS index once per process via `@st.cache_resource`.

---

## 📊 Week 7 Alignment

| LO | Requirement | Evidence in this project |
|---|---|---|
| **7.1** | Implement tool/function calling | Gemini function calling with 2 real tools (RAG search + calculator) |
| **7.2** | Clean schemas & reliable parsing | Docstring-generated schemas; part-level safe parsing of calls/results |
| **7.3** | Short & long-term memory | Rolling 10-message window + persistent JSON facts injected per turn |
| **7.4** | Multi-turn chatbot: RAG + tools + memory | Full integration in both CLI and Streamlit agent loops |
| **7.5** | Errors, retries, loops handled safely | Allow-list, loop guard (4), retry-with-backoff, structured tool errors |

---

## 🔮 Future Enhancements

- Replace the mock `search_web` with Tavily / DuckDuckGo for live data (Lab 7.4 extension).
- Summarization memory for very long conversations.
- Vector-backed long-term memory for unstructured facts (Lab 7.3 extension).
- Parallel tool calls for multi-intent user messages.

---

## 📚 References

- [Anthropic — Tool use (function calling)](https://docs.anthropic.com/en/docs/build-with-claude/tool-use)
- [OpenAI — Function calling guide](https://platform.openai.com/docs/guides/function-calling)
- [LangChain — Memory & message history](https://python.langchain.com/docs/modules/memory/)
- Arabic: Prompt Engineering series (memory loops) + mini-RAG — @bakrianoo
- Helwan Career Center — 12-Week Industry Roadmap, Week 7

---

## Author

**Basmalah Ahmed**
AI Engineer Starter Kit — Week 7